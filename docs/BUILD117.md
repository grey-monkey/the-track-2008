# Build 117 — speed ceiling

Live candidate published from `outputs/painted-prototype/speed117`.

- Speed slider max is now 182%, which is 30% higher than the previous 140% maximum.
- Straight-line target speed uses the full selected pace.
- Turn lookahead uses `turnSpeedFactor`, capped at normal pace when the slider is above 100%, so the car still slows to normal behavior for curves and turns.
- Rider animation Build118 remains a review study only, available at `/animation118-study/`.
